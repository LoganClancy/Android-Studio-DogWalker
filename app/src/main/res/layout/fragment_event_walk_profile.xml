<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp">

    <!-- Image at the top -->
    <ImageView
        android:id="@+id/image_pet"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/petwalker"
        android:layout_gravity="center"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="20dp" />

    <!-- Title centered -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Welcome to Pet Walker!"
        android:textSize="28sp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_marginBottom="40dp" />

    <!-- Buttons aligned vertically, full width -->
    <Button
        android:id="@+id/btn_create_event"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Create Event"
        android:layout_marginBottom="12dp" />

    <Button
        android:id="@+id/btn_join_event"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Join Event"
        android:layout_marginBottom="12dp" />

    <Button
        android:id="@+id/btn_create_walk"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Create Walk"
        android:layout_marginBottom="12dp" />

    <Button
        android:id="@+id/btn_join_walk"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Join Walk"
        android:layout_marginBottom="12dp" />

    <Button
        android:id="@+id/btn_my_activities"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="My Activities"
        android:layout_marginTop="20dp" />

</LinearLayout>